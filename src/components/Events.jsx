// @flow
import React from 'react';

export default class Events extends React.Component {
  props: {
    serverUrl: string,
  };

  render(): React$Element<*> {
    return (
      <div>todo</div>
    );
  }
}
